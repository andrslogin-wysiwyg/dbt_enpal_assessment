version: 2

models:
  - name: stg_pipedrive__activity_types
    description: '{{ doc("stg_pipedrive__activity_types") }}'
    config:
      meta:
        owner: analytics_engineering
        model_criticality: low
    columns:
      - name: activity_type_id
        description: '{{ doc("pd_activity_type_id") }}'
        tests: [not_null, unique]
      - name: activity_type_code
        description: '{{ doc("pd_activity_type_code") }}'
        tests: [not_null, unique]
      - name: activity_type_name
        description: '{{ doc("pd_activity_type_name") }}'
        tests: [not_null]
      - name: is_active
        description: '{{ doc("pd_is_active") }}'
